import factory from '@adonisjs/lucid/factories'
import CartIten from '#models/cart_iten'

export const CartItenFactory = factory
  .define(CartIten, async ({ faker }) => {
    return {}
  })
  .build()
