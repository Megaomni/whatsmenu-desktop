import { test } from '@japa/runner'

test.group('Product service', () => {
  test('Deve ser possível criar um novo produto', async ({ assert }) => {})
  test('Deve ser possível atualizar um novo produto', async ({ assert }) => {})
  test('Deve ser possível remover um novo produto', async ({ assert }) => {})
})
